<% content_for :page_title do %>
  <%= t('.title') %> #<%= @customer.id %>
<% end %>

<%= render 'form', customer: @customer %>

<%= link_to t('action.show'), @customer %> |
<%= link_to t('action.back'), customers_path %>
