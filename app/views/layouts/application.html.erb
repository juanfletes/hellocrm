<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/head" %>
  </head>
  <body class="sidebar-mini fixed">
    <div class="wrapper">
     <%= render "layouts/header" %>
     <%= render "layouts/sidebar" %>
      <div class="content-wrapper">
        <div class="container-fluid">
          <div class="row">
            <div class="main-header">
              <h4><%= content_for :content_header %></h4>
            </div>
          </div>
          <%= yield %>
        </div>
      </div>
      <%= render "layouts/footer" %>
    </div>
  </body>
</html>
